# Apple Music GUI 下载器 – 使用步骤

- 本文档将指导您完成从配置到下载的完整流程。
[自述文件](./README.md)
## 文档须知
- 此使用步骤不会提供**English**文档，若需使用English，请自行翻译，感谢您的理解！
- This Doc haven't English,if you want read **English Version** ,please translate it!
## 前提条件

- 已安装 Python 3.10+ 和所有依赖（参考 README）
- 已安装 FFmpeg（如需转 FLAC）
- 拥有有效的 Apple Music 订阅
- 已从浏览器导出 `cookies.txt`（Netscape 格式）

## 第一步：启动程序
先转到此目录
```bash
cd 你所存放的位置
在终端中运行：
```bash
python apple_music_gui.py
```

程序窗口将打开，界面分为三个区域：配置、任务管理、下载进度。

---

第二步：配置下载参数

1. 选择 Cookies 文件

· 点击“Cookies文件”旁的 浏览 按钮，选择您之前导出的 cookies.txt。
· 确保文件有效（包含正确的认证信息）。

2. 选择输出目录

· 点击“输出目录”旁的 浏览 按钮，选择一个文件夹用于保存下载的音乐文件。

3. 调整下载选项（可选）

· 音质：推荐 aac_256（256kbps），也可选择更低码率。
· 并发数：同时下载的任务数，建议 3~5，过大可能被限制。
· 下载歌词：勾选后会自动下载同步歌词（.lrc）并嵌入元数据。
· 转 FLAC：勾选后会将下载的 AAC（m4a）转码为 FLAC 无损格式（需 FFmpeg）。
· 封面尺寸：填写如 600x600，不填则使用默认尺寸。

💡 所有设置都会自动保存到 config.txt，下次启动自动加载。

---

第三步：添加下载任务

1. 在“任务管理”区域的输入框中粘贴 Apple Music 链接。
   · 支持链接类型：歌曲、专辑、歌单、艺人主页。
   · 示例：https://music.apple.com/cn/album/xxx/123456789
2. 点击 添加到队列，链接会出现在下方的任务列表中。
3. 重复上述操作添加多个任务（队列支持批量下载）。

---

第四步：开始下载

· 点击 开始下载 按钮，程序将依次处理队列中的任务。
· 下载过程中，进度条会显示当前任务的进度，日志区域会实时输出 gamdl 的详细信息。
· 如需取消当前任务，点击 取消当前。

---

第五步：处理下载结果

· 下载完成后，文件会保存在您指定的输出目录中。
· 如果开启了转 FLAC，会生成 .flac 文件；否则为 .m4a（AAC）。
· 若下载失败，程序会弹出提示，并询问是否继续下一个任务。

---

常见问题

Q：提示“Cookies 无效”怎么办？

A：请重新从浏览器导出 Cookies，确保登录状态有效，并且导出格式为 Netscape（不是 JSON）。

Q：转 FLAC 失败？

A：请检查 FFmpeg 是否安装并添加到 PATH，或在终端运行 ffmpeg -version 验证。

Q：如何修改默认配置？

A：可以直接编辑 config.txt，或在 GUI 中修改设置，程序会自动保存。

---

高级技巧

· 批量添加：可事先将多个链接保存为文本文件，逐条复制粘贴。
· 自定义封面尺寸：根据需求调整，如 1200x1200 可获得高清封面。
